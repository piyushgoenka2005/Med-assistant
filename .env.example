# Shared
NODE_ENV=development

# API
API_PORT=4000
UPLOAD_DIR=./storage

# Firebase (Firestore)
# Option A (recommended): point to a service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=C:\\path\\to\\firebase-service-account.json

# Option B: inline service account fields (if you can't use a file)
FIREBASE_PROJECT_ID=replace_me
FIREBASE_CLIENT_EMAIL=replace_me@replace_me.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nREPLACE_ME\n-----END PRIVATE KEY-----\n

# Web
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# OCR + LLM (extraction)
# OCR.Space API key
OCR_SPACE_API_KEY=replace_me

# OpenRouter (OpenAI-compatible) API key + model
OPENROUTER_API_KEY=replace_me
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_MODEL=nvidia/nemotron-nano-9b-v2:free
# Optional but recommended by OpenRouter
OPENROUTER_SITE_URL=http://localhost:3000
OPENROUTER_APP_NAME=medi-platform

# Pharmacy backend (adapter)
PHARMACY_BASE_URL=https://pharmacy.example
PHARMACY_API_KEY=replace_me

# Pathway (optional): real-time pricing/delivery quoting
# If unset, cart totals are computed locally from dummy vendors.
PATHWAY_BASE_URL=http://localhost:8080

# LLM (OpenRouter) for prescription extraction
# OpenRouter is OpenAI-compatible, but uses its own API key and base URL.
OPENROUTER_API_KEY=replace_me
# Pick a model you have access to on OpenRouter (examples):
#   openai/gpt-4o-mini
#   anthropic/claude-3.5-sonnet
OPENROUTER_MODEL=openai/gpt-4o-mini
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# Optional but recommended by OpenRouter policies:
OPENROUTER_SITE_URL=http://localhost:3000
OPENROUTER_APP_NAME=medi-platform

# ElevenLabs (voice reminders/check-ins)
ELEVENLABS_API_KEY=replace_me
ELEVENLABS_VOICE_ID=replace_me

# Google Calendar (optional)
# Share the target calendar with the service account email you use.
GOOGLE_CLIENT_EMAIL=replace_me@replace_me.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nREPLACE_ME\n-----END PRIVATE KEY-----\n
GOOGLE_CALENDAR_ID=primary
